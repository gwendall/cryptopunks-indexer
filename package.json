{
  "name": "indexer",
  "version": "1.0.0",
  "description": "",
  "main": "index.js",
  "scripts": {
    "test": "echo \"Error: no test specified\" && exit 1",
    "start": "node src/index.js",
    "sync": "node src/index.js sync",
    "export": "node src/index.js export",
    "export:ops": "node src/index.js export-ops",
    "export:events": "node src/index.js export-events",
    "docker:up": "docker compose up -d",
    "docker:logs": "docker compose logs -f",
    "docker:down": "docker compose down",
    "rebuild:sqlite": "npm rebuild better-sqlite3 || pnpm rebuild better-sqlite3 || yarn rebuild better-sqlite3",
    "postinstall": "node scripts/postinstall.cjs",
    "fix:pnpm": "pnpm approve-builds better-sqlite3 && pnpm rebuild better-sqlite3"
  },
  "keywords": [],
  "author": "",
  "license": "ISC",
  "type": "module",
  "engines": {
    "node": ">=18"
  },
  "dependencies": {
    "better-sqlite3": "^12.2.0",
    "dotenv": "^17.2.2",
    "ethers": "^6.15.0",
    "picocolors": "^1.0.0"
  }
}
