{
  "name": "indexer",
  "version": "1.0.0",
  "description": "",
  "main": "src/index.ts",
  "scripts": {
    "test": "echo \"Error: no test specified\" && exit 1",
    "start": "tsx src/server.ts",
    "serve": "node scripts/serve.cjs",
    "restart": "node scripts/serve.cjs",
    "pm2:start": "pm2 start ecosystem.config.cjs --only punks-indexer",
    "pm2:restart": "pm2 startOrRestart ecosystem.config.cjs --only punks-indexer --update-env",
    "pm2:reload": "pm2 reload punks-indexer --update-env || pm2 start ecosystem.config.cjs --only punks-indexer",
    "pm2:stop": "pm2 stop punks-indexer || true",
    "pm2:logs": "pm2 logs punks-indexer",
    "pm2:save": "pm2 save",
    "pm2:startup": "pm2 startup",
    "pm2:status": "pm2 ls",
    "sync": "tsx src/index.ts sync",
    "export": "tsx src/index.ts export",
    "export:ops": "tsx src/index.ts export-ops",
    "export:events": "tsx src/index.ts export-events",
    "backfill:timestamps": "tsx src/index.ts backfill-ts",
    "stop": "node scripts/stop-server.cjs",
    "type-check": "tsc --noEmit",
    "docker:up": "docker compose up -d",
    "docker:logs": "docker compose logs -f",
    "docker:down": "docker compose down",
    "rebuild:sqlite": "npm rebuild better-sqlite3 || pnpm rebuild better-sqlite3 || yarn rebuild better-sqlite3",
    "postinstall": "node scripts/postinstall.cjs",
    "fix:pnpm": "pnpm approve-builds better-sqlite3 && pnpm rebuild better-sqlite3"
  },
  "keywords": [],
  "author": "",
  "license": "ISC",
  "type": "module",
  "engines": {
    "node": ">=18"
  },
  "dependencies": {
    "better-sqlite3": "^12.2.0",
    "dotenv": "^17.2.2",
    "ethers": "^6.15.0",
    "picocolors": "^1.0.0",
    "ws": "^8.18.0"
  },
  "devDependencies": {
    "@types/better-sqlite3": "^7.6.13",
    "@types/node": "^22.18.0",
    "tsx": "^4.20.5",
    "typescript": "^5.9.2"
  }
}
